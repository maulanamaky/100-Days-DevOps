 apiVersion: v1
kind: Pod
metadata:
  labels:
    run: secret-datacenter
  name: secret-datacenter
spec:
  containers:
  - image: fedora:latest
    name: secret-container-datacenter
    command: ["sleep", "3600"]
    volumeMounts:
    - name: secret-conf
      mountPath: /opt/apps
  volumes:
  - name: secret-conf
    secret:
      secretName: official
